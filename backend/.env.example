# Variables de entorno para Backend

# Puerto del servidor
PORT=5000
NODE_ENV=development

# Base de datos PostgreSQL (AWS RDS)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=elearning
DB_USER=postgres
DB_PASSWORD=tu_password

# JWT Secret
JWT_SECRET=tu_secreto_super_seguro_cambiame_en_produccion_2024

# URL del servidor de videos (Cloudflare Tunnel)
VIDEO_SERVER_URL=http://localhost:8080

# CORS Origins permitidos
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
